# September 12 revision checks

- Canonical LaTeX build: 5 main pages, 8 total, 150-word abstract. No overfull boxes, missing references or glyph warnings. The source ZIP also compiled independently after extraction.
- PDF and vector figures inspected visually; the current source is `report/latex/main.tex`. Template coverage is explicit, including differences from the native DOCX layout.
- Python: 94 tests passed. The local HTTP-stub test requires loopback socket permission; it passed when run outside the filesystem/network sandbox. No inference or external model calls.
- JavaScript: both web test files passed. The committed web data regenerates without changes.
- Browser: all four pages checked at 320, 768 and 1440 CSS pixels, with local fonts loaded and no horizontal page overflow. Preset selection, playback, case progression, model selection and representation tabs checked separately.
- First-contact analysis: 384 existing episodes, worker A before responses, 32 condition cells. Source hashes and post-hoc status accompany the outputs. A CI step checks the derived tables/counts against retained records.
- Shared-library extension: 80 development and 480 held-out episodes; all 560 replay-verified against retained requests, decisions, snapshots and local freeze. Eight budget-truncated responses remain included. Separate isolated diagnostic: 144 calls, verified against the oracle and pre-call freeze.
- Fresh-input standard Qwen3.8-27B screen: 26/36, 13/18 per family, no invalid format; missed the preselected 34/36 gate. All 36 calls, weight hash and configuration retained and verified separately.
- Browser extension: 640 episode views across the three widths, both capacity panels and all routes; no runtime errors or horizontal overflow.
- New institutional comparisons (actual humans, rotation with feedback, strategic use, sanctions, interruption incentives) remain proposed. Neither an Apart submission nor independent human verification is asserted.

Swarm-scale intake under finite human capacity is explicitly deferred until after the hackathon. The present three-worker study cannot estimate that value. Amodei’s essay supplies institutional motivation, not experimental validation.

## Focused manuscript revision

The body now contains one empirical investigation with capability controls. The original frozen analysis is preserved; a separate retrospective validity analysis supplies the main report contrast and the valid/faulty reuse/report figure. Three additional tests check blocked-attempt counting, pre-response contact and pairing direction/coverage. Source-linked examples are generated from retained events. The PDF has one verification disclosure and no author-facing instructions. The source ZIP now includes only assets used by the current manuscript.
